name: Deploy Dutch Municipal Job-Home Spider Network Atlas

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release chunks are being staged; deployment will be enabled in the final commit."
